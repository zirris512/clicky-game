import React from "react";

function Header() {
    return (
        <header className="text-center">
            <h1>Clicky Game~!</h1>
            <h2>Click an image to earn points, but don't click an image more than once!</h2>
        </header>
    )
}

export default Header;